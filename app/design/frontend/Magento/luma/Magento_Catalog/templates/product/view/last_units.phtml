<?php
$product = $block->getProduct();
if ($product && $product->getData('show_last_units_tag')): ?>
    <div class="last-units-tag">
        Últimas Unidades - Aproveite!
    </div>
<?php endif; ?>
